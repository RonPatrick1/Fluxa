# Loaded in nginx's http context. Keep this separate from the server location
# include because limit_req_zone is not valid inside a server block.
limit_req_zone $binary_remote_addr zone=fluxa_login:10m rate=5r/m;
